

export default async function page() {

    return (
      <main className="font-sans h-screen overflow-hidden flex bg-accent">
            {
                "project"
            }
      </main>
    );
  }